syntax = "proto3";

option go_package = "./proto";

package proto;

message Empty {}

message MessageResponse {
	uint32 id = 1;
	string msg = 2;
}

service Message {
 rpc Subscribe(Empty) returns (stream MessageResponse) {}
}
